/**
 * Main App Entry Point
 * Initialisiert die Anwendung
 */
document.addEventListener('DOMContentLoaded', () => {
    // Alle Module sind bereits initialisiert
    // GameManager wird in gameManager.js instanziiert
    // StandingsCalculator wird in standingsCalculator.js instanziiert
    // UIRenderer wird in uiRenderer.js instanziiert
    console.log('🏑 UHC Hamburg mu10 Spieltagsrechner lädt erfolgreich!');
});